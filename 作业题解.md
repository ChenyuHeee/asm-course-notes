# 汇编语言程序设计与调试 — 作业题解

> 共 8 次作业 17 道题，逐题给出答案 + 解题思路。

---

## 第1次作业

### 1-1. AX为正时计算 2*AX，否则计算 AX 的平方

**考点**：有符号比较 + 条件跳转 + 乘法

```asm
;#1_begin--------------------------------------
   cmp ax, 0
   jge positive       ; ax >= 0 → 跳去乘2
negative:
   imul ax            ; ax < 0 → ax = ax * ax（结果在 dx:ax，取 ax）
   jmp exit
positive:
   imul ax, 2         ; ax = ax * 2
;#1_end========================================
```

**思路**：`cmp ax, 0` 后 `jge` 判断正负。有符号数用 `imul`。`imul ax` 单操作数结果在 `dx:ax`，但 `ax` 是平方的低16位（题目只要求 AX，高位丢掉没问题）。

---

### 1-2. 判断素数

**考点**：除法 + 循环 + 标志位判断

```asm
;#1_begin--------------------------------------
   cmp cx, 2
   jb not_prime        ; cx < 2 → 不是素数
   mov bx, 2
again:
   cmp bx, cx
   jae is_prime        ; bx >= cx → 试除完毕，是素数
   mov ax, cx
   mov dx, 0           ; 16位/16位 → 清零 dx
   div bx              ; ax = 商, dx = cx % bx
   cmp dx, 0
   je not_prime        ; 整除 → 不是素数
   add bx, 1
   jmp again
is_prime:
   mov ax, 1
   jmp exit
not_prime:
   mov ax, 0
;#1_end========================================
```

**思路**：bx 从 2 试除到 cx-1，`dx = cx % bx`，余数为 0 则不是素数。注意 `cx < 2` 边界处理。

---

## 第2次作业

### 2-1. 输入大写字母，输出从该字母到 Z

**考点**：字符 I/O + 循环

```asm
;#1_begin-------------------------------------
   mov ah, 1
   int 21h            ; al = 输入字母
   mov bl, al          ; 备份 al 到 bl
next:
   mov ah, 2
   mov dl, bl          ; 输出当前字母
   int 21h
   cmp bl, 'Z'
   je done
   add bl, 1
   jmp next
done:
;#1_end=======================================
```

**思路**：输入 → 循环输出直到 'Z'。

---

### 2-2. 菱形（正奇数 n 行星号）

**考点**：除法 + 绝对值 + 函数调用

```asm
;#1_begin-------------------
   mov ax, bx
   mov dx, 0
   mov bp, 2
   div bp              ; ax = 行数/2（向下取整，因为奇数）
   mov di, ax          ; di = rows/2
   mov si, 0
   sub si, ax          ; si = -(rows/2)
;#1_end=====================

;#2_begin-------------------
   mov bp, si
   cmp bp, 0
   jge bp_is_positive
   neg bp              ; bp = abs(si)
;#2_end=====================

;#3_begin-------------------
   mov bp, si
   cmp bp, 0
   jge skip_neg
   neg bp
skip_neg:
   add bp, bp          ; bp = 2*abs(si)
   mov ax, bx
   sub ax, bp          ; ax = bx - 2*abs(si)
   mov bp, ax          ; bp = 星号数
;#3_end=====================
```

**思路**：
- 第1空：`si` 从 `-(rows/2)` 开始，`di = rows/2` 是上限。si 从负到正遍历每一行。
- 第2空：空格数 = |si|（负数行号时 abs 得到空格数，正数同理）。
- 第3空：星号数 = `bx - 2*|si|`（总行数 - 2×偏移量）。

以 n=5 为例，si 遍历 `-2, -1, 0, 1, 2`，空格 `2,1,0,1,2`，星号 `1,3,5,3,1`。

---

## 第3次作业

### 3-1. 判断 eax 是否「自身是奇数，且各位数字之和也是奇数」

**考点**：32位除法 + 循环拆数字 + 奇偶判断

```asm
;#1_begin--------------------------------------
   test eax, 1
   jz no               ; eax 是偶数 → 'N'

   mov ecx, 0           ; ecx = 各位数字之和
digit_loop:
   cmp eax, 0
   je check_sum
   mov edx, 0
   div ebx             ; edx:eax / 10 → eax=商, edx=余数（=最低位数字）
   add ecx, edx         ; 累加到 ecx
   jmp digit_loop
check_sum:
   test ecx, 1
   jz no                ; ecx 是偶数 → 'N'
   jmp yes              ; ecx 是奇数 → 'Y'
;#1_end========================================
```

**思路**：`test eax, 1` 检测 eax 是否为奇数。然后循环 `div ebx`（ebx=10）拆出个位数字累加到 ecx，最后 `test ecx, 1` 检测和的奇偶。

---

### 3-2. eax 中最左侧 1 位和最右侧 1 位的位号之差

**考点**：移位 + 位运算 + 十进制输出

```asm
;#1_begin--------------------------------------
   ; --- 找最左侧的 1 ---
   mov ecx, 31
find_left:
   mov ebx, 1
   shl ebx, cl         ; ebx = 1 << ecx（掩码移到当前位）
   test eax, ebx
   jnz found_left
   sub ecx, 1
   jge find_left
   ; ecx < 0 → 没找到 1 → 输出0
   mov dl, '0'
   mov ah, 2
   int 21h
   jmp done
found_left:
   mov esi, ecx         ; esi = 最左侧 1 的位号

   ; --- 找最右侧的 1 ---
   mov ecx, 0
find_right:
   mov ebx, 1
   shl ebx, cl
   test eax, ebx
   jnz found_right
   add ecx, 1
   cmp ecx, 31
   jle find_right
   ; 没找到 1
   mov dl, '0'
   mov ah, 2
   int 21h
   jmp done
found_right:
   mov edi, ecx         ; edi = 最右侧 1 的位号

   ; --- 计算差值并输出 ---
   mov eax, esi
   sub eax, edi          ; eax = 位号之差
   ; 把 eax 转十进制输出
   mov ecx, 0
push_loop:
   mov edx, 0
   mov ebx, 10
   div ebx              ; eax / 10 → eax=商, edx=余数
   push edx
   inc ecx
   cmp eax, 0
   jne push_loop
pop_loop:
   pop edx
   add dl, '0'
   mov ah, 2
   int 21h
   loop pop_loop
;#1_end========================================
```

**思路**：用移位掩码分别从高位扫描（找最左1）和从低位扫描（找最右1），差值转十进制输出。

---

## 第4次作业

### 4-1. 两头对称交换法倒置字符串

**考点**：双指针 + 字符交换

```asm
;#1_begin------------------
swap_loop:
   cmp si, di
   jge output           ; si >= di → 交换完成
   mov al, ds:[si]
   mov ah, ds:[di]
   mov ds:[si], ah
   mov ds:[di], al
   add si, 1
   sub di, 1
   jmp swap_loop
;#1_end====================
```

**思路**：si 指向首字符，di 指向末字符。当 `si < di` 时交换 `s[si]` 和 `s[di]`，si 后移、di 前移，直到相遇。

---

### 4-2. 金字塔

**考点**：嵌套循环 + 空格/字符输出

```asm
; 输入 c，n = c - '0'，输出 n 层金字塔
   mov ah, 1
   int 21h            ; al = 输入字符 c
   mov dl, al          ; ★ 先保存原始字符 c 到 dl
   sub al, '0'        ; al = n
   mov cl, al          ; cl = n（总行数）
   mov ch, 0
   mov bx, 1           ; bx = 当前行号（从1开始）
next_row:
   ; 输出 n - bx 个空格
   push cx
   mov cl, al          ; cl = n（总行数，al 中仍是 n）
   mov ch, 0
   sub cx, bx           ; cx = n - 当前行号 = 空格数
space_loop:
   push ax
   push dx
   mov ah, 2
   mov dl, ' '
   int 21h
   pop dx
   pop ax
   loop space_loop
   ; 输出 2*bx - 1 个字符
   mov cx, bx
   shl cx, 1
   sub cx, 1           ; cx = 2*bx - 1
char_loop:
   push ax
   mov ah, 2              ; dl 已是要输出的字符
   int 21h
   pop ax
   loop char_loop
   ; 换行
   mov ah, 2
   mov dl, 0Dh
   int 21h
   mov ah, 2
   mov dl, 0Ah
   int 21h
   add bx, 1
   pop cx
   dec cx
   jnz next_row
```

**思路**：第 i 行（1-based）有 `n-i` 个空格 + `2i-1` 个字符。总共 n 行。

---

### 4-3. 逆序输出字符串

**考点**：字符串遍历 + 反向输出

```asm
;#1_begin-------------------------
   ; 先找到字符串末尾（'\0'的位置）
   mov si, offset s
find_end:
   cmp byte ptr ds:[si], 0
   je found
   add si, 1
   jmp find_end
found:
   ; si 指向 '\0'，往前输出
   sub si, 1
output_loop:
   cmp si, offset s
   jb exit             ; si < s 的首地址 → 结束
   mov ah, 2
   mov dl, ds:[si]
   int 21h
   sub si, 1
   jmp output_loop
;#1_end===========================
```

**思路**：先扫描到末尾的 `\0`，然后从最后一个字符往前逐个输出。

---

### 4-4. 提取十六进制字符

**考点**：字符分类 + 大小写转换

```asm
; 输入一行到 s，提取十六进制字符到 t
   mov si, offset s
   mov di, offset t
scan_loop:
   mov al, ds:[si]
   cmp al, 0
   je scan_done         ; 字符串结束
   ; 判断是否 '0'-'9'
   cmp al, '0'
   jb next_char
   cmp al, '9'
   jbe save_char
   ; 判断是否 'A'-'F'
   cmp al, 'A'
   jb next_char
   cmp al, 'F'
   jbe save_char
   ; 判断是否 'a'-'f'
   cmp al, 'a'
   jb next_char
   cmp al, 'f'
   ja next_char
   ; 小写 → 大写
   sub al, 20h         ; 'a' - 'A' = 20h
save_char:
   mov ds:[di], al
   add di, 1
next_char:
   add si, 1
   jmp scan_loop
scan_done:
   mov byte ptr ds:[di], 0  ; t 以 '\0' 结尾
```

**思路**：逐字符判断是否在 `0-9`、`A-F`、`a-f` 范围内，小写字母减 `20h` 转大写，合法字符写入 t，最后补 `\0`。

---

## 第5次作业

### 5-1. 写显卡内存输出 ASCII 字符及十六进制 ASCII 码

**考点**：显存直接写 + 十六进制转换

```asm
; buf[0], buf[1] = 两个十六进制数字字符
; 先解码得到 c
   mov al, buf[0]
   cmp al, '9'
   jbe digit_hi
   sub al, 'A' - 10
   jmp hi_done
digit_hi:
   sub al, '0'
hi_done:
   mov ah, 0
   mov bl, 16
   mul bl               ; al = buf[0]值 * 16

   mov bh, buf[1]
   cmp bh, '9'
   jbe digit_lo
   sub bh, 'A' - 10
   jmp lo_done
digit_lo:
   sub bh, '0'
lo_done:
   add al, bh           ; al = c = 完整ASCII码
   mov cl, al           ; cl = c

   ; 在显存 0xB800:0000 输出 16 行
   mov ax, 0B800h
   mov es, ax
   mov di, 0            ; 显存偏移
   mov bx, 0            ; 行号 i
row_loop:
   cmp bx, 16
   jge done
   ; --- 输出字符 c+i ---
   mov al, cl
   add al, bl           ; al = c + i
   mov ah, 07h          ; 属性 = 白色
   mov es:[di], ax
   add di, 2
   ; --- 输出空格 ---
   mov al, ' '
   mov ah, 07h
   mov es:[di], ax
   add di, 2
   ; --- 输出十六进制 ASCII 码 ---
   mov al, cl
   add al, bl           ; al = c + i
   mov dl, al           ; ★ 用 dl 暂存原值，不污染 ah
   shr al, 4            ; 高 4 位
   call to_hex_char     ; al = 十六进制字符
   mov ah, 07h          ; 属性 = 白色
   mov es:[di], ax
   add di, 2
   mov al, dl           ; 恢复 c+i
   and al, 0Fh           ; 低 4 位
   call to_hex_char
   mov ah, 07h
   mov es:[di], ax
   add di, 2
   ; --- 换行（跳过本行剩余显存位置）---
   mov ax, 160
   sub ax, 8            ; 一行160字节，用了4个字符=8字节
   add di, ax
   add bx, 1
   jmp row_loop
```

> 篇幅所限，`to_hex_char` 和完整显存定位逻辑省略，考场上请提前准备好转十六进制+显存写入的模板函数。

### 5-2. 十进制数 + 十六进制数求和

**考点**：字符串 → 整数转换（两种进制）+ 大数运算

**十进制字符串转整数**：
```asm
; 字符串 d[] → eax
   xor eax, eax
   mov si, offset d
dec_loop:
   movzx ebx, byte ptr ds:[si]
   cmp bl, 0Dh          ; 回车结束
   je dec_done
   cmp bl, 0
   je dec_done
   sub bl, '0'
   mov edx, 10
   mul edx              ; eax = eax * 10
   add eax, ebx
   inc si
   jmp dec_loop
dec_done:
   mov result, eax
```

**十六进制字符串转整数**：
```asm
; 字符串 h[] → ebx（注意：不能用 eax 读字符，eax 存了十进制结果！）
   xor ebx, ebx
   mov si, offset h
hex_loop:
   movzx ecx, byte ptr ds:[si]   ; 用 ecx 而非 eax，保护十进制结果
   cmp cl, 0Dh
   je hex_done
   cmp cl, '9'
   jbe hex_digit
   cmp cl, 'a'
   jb hex_upper
   sub cl, 'a' - 10     ; 小写 a-f
   jmp hex_add
hex_upper:
   sub cl, 'A' - 10     ; 大写 A-F
   jmp hex_add
hex_digit:
   sub cl, '0'
hex_add:
   shl ebx, 4           ; ebx = ebx * 16
   add ebx, ecx
   inc si
   jmp hex_loop
hex_done:
```

**求和**：`add eax, ebx; mov result, eax`

### 5-3. 8x16 点阵显示

**考点**：图形模式 + 数组偏移计算 + 逐bit画点

```asm
;#1_begin-------------------------------------
   mov si, 0            ; i = 0
char_loop:
   mov al, buf[si]
   cmp al, 0
   je exit
   sub al, '0'          ; al = 数字值
   mov ah, 0            ; ax = value
   mov di, 16
   mul di               ; ax = value * 16
   mov di, ax
   add di, offset matrix ; di = 点阵数据起始偏移

   ; 在图形模式画 8x16 点阵
   ; 屏幕位置: (si * 8, 0)，即每个字符占 8 像素宽
   mov bx, si
   shl bx, 3            ; bx = i * 8（X 坐标）
   mov dx, 0            ; dx = Y 坐标
draw_char_8x16:
   push bx
   mov cx, 16           ; 16 行
draw_row:
   mov al, ds:[di]      ; 该行的 8 个 bit
   push cx
   mov cx, 8
draw_col:
   ; 逐 bit 检测并画点
   test al, 80h
   jz pixel_off
   ; 画亮点
   call put_pixel
pixel_off:
   shl al, 1
   inc bx               ; X++
   loop draw_col
   pop cx
   inc di
   sub bx, 8
   inc dx               ; Y++
   loop draw_row
   pop bx
   inc si
   jmp char_loop
;#1_end=======================================
```

> 完整点阵绘制涉及 `put_pixel` 函数（计算 A000 段偏移 + bit 掩码），考试时可以把点阵画法预先封装好。

---

## 第6次作业

### 6-1. 二进制字符串转整数

**考点**：字符串解析 + 移位累加

```asm
;#1_begin-------------------------------------
   xor ax, ax            ; ax = 结果
   mov si, 0
convert_loop:
   mov dl, buf[si]
   cmp dl, 0
   je convert_done
   shl ax, 1            ; ax = ax * 2（左移一位，腾出最低位）
   sub dl, '0'          ; dl = 0 或 1
   add al, dl           ; 最低位 = dl
   inc si
   jmp convert_loop
convert_done:
   mov abc, ax           ; 保存结果
;#1_end=======================================
```

**思路**：每读一个 bit 字符，`ax = ax * 2 + (bit - '0')`。用 `shl ax, 1` 实现乘 2，用 `add al, dl` 填入新位。

---

### 6-2. 九九乘法表

**考点**：嵌套循环 + 字符变量修改 + 除法拆数字

```asm
;#1_begin-------------------------------------
   mov ah, '1'          ; 被乘数从 '1' 开始
outer:
   mov s[0], ah          ; s[0] = 被乘数字符
   mov s[2], ah          ; s[2] = 乘数从被乘数开始
inner:
   ; 计算乘积
   mov al, s[0]
   sub al, '0'
   mov bl, s[2]
   sub bl, '0'
   mul bl               ; al = 乘积
   ; 拆分十位和个位
   mov ah, 0
   mov bl, 10
   div bl               ; al = 十位, ah = 个位
   cmp al, 0
   jne tens_exists
   mov s[4], ' '         ; 十位为0 → 空格
   jmp units
tens_exists:
   add al, '0'
   mov s[4], al
units:
   add ah, '0'
   mov s[5], ah
   ; 输出算式
   mov ah, 9
   mov dx, offset s
   int 21h
   ; 乘数 +1
   inc byte ptr s[2]
   cmp s[2], '9'
   jbe inner
   ; 换行
   mov ah, 9
   mov dx, offset cr
   int 21h
   ; 被乘数 +1
   inc byte ptr s[0]
   mov ah, s[0]
   mov s[2], ah
   cmp s[0], '9'
   jbe outer
;#1_end========================================
```

**思路**：用 s 数组作为输出模板，`s[0]` 是被乘数，`s[2]` 是乘数，`s[4]` 和 `s[5]` 放乘积的十位和个位。外循环控制被乘数，内循环控制乘数。每次输出调用 `int 21h / ah=9`。

---

### 6-3. 十六进制字符串转 32 位整数

**考点**：十六进制解析 + 移位累加

```asm
; 输入十六进制字符串 s[] → result (dword)
   xor eax, eax
   mov si, offset s
hex_loop:
   movzx ecx, byte ptr ds:[si]
   cmp cl, 0
   je hex_done
   cmp cl, '9'
   jbe hex_digit
   cmp cl, 'a'
   jb hex_upper
   sub cl, 'a' - 10
   jmp hex_shift
hex_upper:
   sub cl, 'A' - 10
   jmp hex_shift
hex_digit:
   sub cl, '0'
hex_shift:
   shl eax, 4           ; eax = eax * 16
   add eax, ecx
   inc si
   jmp hex_loop
hex_done:
   mov result, eax
```

**思路**：每次 `eax = eax * 16 + 新数字`，用 `shl eax, 4` 代替乘16。

---

### 6-4. 16位整数「两头向中间」输出二进制

**考点**：位提取 + 特定顺序输出

**算法**：对于 i=0..7，`buf[2i] = bit(15-i), buf[2i+1] = bit(i)`

以 1234h = `00010010 00110100` 为例：
- i=0: 取 bit15(0) + bit0(0) → buf="00"
- i=1: 取 bit14(0) + bit1(0) → buf="0000"
- i=2: 取 bit13(0) + bit2(1) → buf="000001"
- ...以此类推

```asm
;#1_begin-------------------------------------
   mov si, 0            ; i = 0
bit_loop:
   mov ax, abc          ; 每次重新加载原值（因为 shr 会修改 ax）
   ; --- 取左侧位：bit (15-i) ---
   mov cl, 15
   sub cl, sil          ; cl = 15 - i
   shr ax, cl           ; ax 右移 (15-i) 位，bit0 = 目标位
   and al, 1
   add al, '0'
   mov buf[si*2], al    ; buf[2i] = bit(15-i)
   ; --- 取右侧位：bit i ---
   mov ax, abc          ; 重新加载原值
   mov cl, sil          ; cl = i
   shr ax, cl           ; ax 右移 i 位
   and al, 1
   add al, '0'
   mov buf[si*2+1], al  ; buf[2i+1] = bit(i)
   inc si
   cmp si, 8
   jb bit_loop
;#1_end=======================================
```

---

## 第8次作业

### 8-1. 迷宫 DFS

**考点**：递归 + DFS + 显存操作 + 综合能力

---

**Blank 1: show_maze**

```asm
;#1_begin------------------------------
   push bp               ; ★ 必须有 prologue
   mov bp, sp
   mov dx, [bp+6]        ; dx = y0
show_row:
   cmp dx, [bp+0Ah]      ; y > y1?
   jg show_maze_done
   mov cx, [bp+4]        ; cx = x0
show_col:
   cmp cx, [bp+8]        ; x > x1?
   jg show_col_done
   ; 调用 index(y, x): 先 push x，再 push y（index 用 ret 4 自清）
   push cx               ; push x（→[bp+6]）
   push dx               ; push y（→[bp+4]）
   call index            ; index 用 ret 4，调用者不需要 add sp
   cmp byte ptr ds:[di + offset maze], 1
   jne show_next
   ; 是墙，调用 draw_char(x, y, WALL, WHITE)
   ; draw_char 是 cdecl: push color→shape→y→x
   push 07h              ; color = WHITE
   push 0B2h             ; shape = WALL
   push dx               ; y
   push cx               ; x
   call draw_char
   add sp, 8             ; cdecl: 调用者清理 4 个参数
show_next:
   inc cx
   jmp show_col
show_col_done:
   inc dx
   jmp show_row
show_maze_done:
   pop bp
   ret
;#1_end================================
```

---

**Blank 2: move_bug (DFS)**

C 语言的 DFS 翻译为汇编。注意：index 使用 stdcall（ret 4），调用者不能 add sp。

```asm
;#2_begin------------------------------
   push bp
   mov bp, sp
   sub sp, 6               ; k=[bp-6], nx=[bp-4], ny=[bp-2]

   ; --- 边界检查 ---
   cmp word ptr [bp+4], 0
   jl move_fail
   cmp word ptr [bp+4], RIGHT_BOUND
   jg move_fail
   cmp word ptr [bp+6], 0
   jl move_fail
   cmp word ptr [bp+6], BOTTOM_BOUND
   jg move_fail

   ; --- 检查 maze + mark（index 用 ret 4，不需 add sp）---
   push word ptr [bp+4]    ; x（→[bp+6]）
   push word ptr [bp+6]    ; y（→[bp+4]）
   call index              ; ret 4 自动清参
   cmp byte ptr ds:[di + offset maze], 1
   je move_fail
   cmp byte ptr ds:[di + offset mark], 1
   je move_fail

   ; --- 标记已访问 ---
   mov byte ptr ds:[di + offset mark], 1
   mov byte ptr ds:[di + offset trace], 1

   ; --- 到达终点? ---
   mov ax, [bp+4]
   cmp ax, [bp+8]
   jne try_dirs
   mov ax, [bp+6]
   cmp ax, [bp+0Ah]
   jne try_dirs
   mov ax, 1
   jmp move_done

try_dirs:
   mov word ptr [bp-6], 0  ; k = 0
dir_loop:
   cmp word ptr [bp-6], 4
   jge move_fail

   ; --- 计算 nx = x0 + d[k][0], ny = y0 + d[k][1] ---
   mov bx, [bp-6]          ; bx = k
   shl bx, 2               ; bx = k*4
   mov ax, word ptr [bx + offset d]     ; dx
   add ax, [bp+4]
   mov [bp-4], ax          ; nx = x0 + dx
   mov ax, word ptr [bx + offset d + 2] ; dy
   add ax, [bp+6]
   mov [bp-2], ax          ; ny = y0 + dy

   ; --- 递归调用（cdecl: 右→左压栈）---
   push word ptr [bp+0Ah]  ; y1（右→[bp-8]）
   push word ptr [bp+8]    ; x1（→[bp-10]）
   push word ptr [bp-2]    ; ny（读[ny]→[bp-12]）
   push word ptr [bp-4]    ; nx（读[nx]→[bp-14]）
   call move_bug
   add sp, 8               ; cdecl 调用者清参

   cmp ax, 1
   je move_done

   inc word ptr [bp-6]     ; k++
   jmp dir_loop

move_fail:
   mov ax, 0
move_done:
   mov sp, bp
   pop bp
   ret
;#2_end================================
```

**方向数组 d**（模板中定义为 `dw`）：
```asm
d dw -1, 0    ; 上 (dx=-1, dy=0)
  dw  0, 1    ; 右 (dx=0,  dy=1)
  dw  1, 0    ; 下 (dx=1,  dy=0)
  dw  0, -1   ; 左 (dx=0,  dy=-1)
```

---

## 考点速查表

| 作业 | 考点 |
|---|---|
| 1-1 | `cmp` + `jxx` 分支、`imul` 有符号乘法 |
| 1-2 | `div` 除法、循环、素数算法 |
| 2-1 | 字符 I/O（`ah=1`/`ah=2`）、循环 |
| 2-2 | 除法 `div`、`neg` 取绝对值、函数 `call`/`ret`、菱形数学公式 |
| 3-1 | 32位 `div`、循环拆数字、`test` 奇偶判断 |
| 3-2 | `shl`/`test` 位扫描、十进制输出 |
| 4-1 | 双指针、字符交换、条件循环 |
| 4-2 | 嵌套循环、空格/字符公式 |
| 4-3 | 字符串扫描到`\0`、反向遍历 |
| 4-4 | 字符范围判断、大小写转换 |
| 5-1 | 显存直接写入（B800段）、十六进制转换 |
| 5-2 | 十进制/十六进制字符串→整数、`mul`/`shl` |
| 5-3 | 图形模式（A000段）、点阵绘制、偏移计算 |
| 6-1 | 二进制字符串→整数、`shl`左移累加 |
| 6-2 | 嵌套循环、除法拆分数字、字符数组模板 |
| 6-3 | 十六进制字符串→32位整数、`shl` |
| 6-4 | 位提取、双向位号计算 |
| 8-1 | DFS递归、显存绘制、多文件组织、综合应用 |
